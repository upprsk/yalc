module main;

def Counter = struct {
    cnt:   i32 = 0,
    total: i32,
};

func Counter.next( c: *Counter) (i32, bool) {
    if c.cnt == c.total {
        return c.cnt, false;
    }

    var v = c.cnt;
    c.cnt += 1;
    return v, true;
}
