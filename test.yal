module main;

func get_pair(x: i32, y: i32) (i32, i32) {
    return x, y;
}

@export
func main() i32 {
    var x, y: i32, i32 = get_pair(10, 20);
    var x, y, z: i32, i32, i32 = get_pair(10, 20), 0;
    var x, _    = get_pair(10, 20);
    var x, y, z = get_pair(10, 20), 0;

    var i: i32;

    return 0;
}
