module main;

func main(argc: i32, argv: [*][*]u8) i32 {
    var i = 0;
    while i < argc {
        defer i = i + 1;
        c_printf("- %d: %s\n".ptr, i, argv[i]);
    }

    return 0;
}

// vim: ft=yal
