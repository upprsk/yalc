// f := .() -> i32 { return 42; };
// g := .(x: i32) -> i32 { return x * f(); };
// h := .() -> i32 { return 69; };

update := .(n: *i32) {
    n.* = n.* + 1;
};

main := .() {
    // a := f();
    // b := -g(h());
    // c := a + b / 2;
};
