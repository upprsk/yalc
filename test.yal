module main;

def Args = struct {
    argc: i32,
    argv: [*][*]u8,
};

func print_args(args: *Args) {
}

func main(argc: i32, argv: [*][*]u8) i32 {
    var args: Args = .{ .argc=argc, .argv=argv };

    print_args(&args);
    return 0;
}

// vim: ft=yal
