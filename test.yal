Pair := .(T: type) -> type {
    return record {
        first: T;
        second: T;
    };
};

main := .() {
    p: Pair(i32) = .{.first = 0, .second = 0};
    printf("p=.{.first=%d, .second=%d}\n", p.first, p.second);
};

extern "printf" printf: .(fmt: [*]i8, ...) -> void;
