func divmod(a: i32, b: i32) (i32, i32) {
    return a / b, a % b;
}

func add_inplace(x: *i32, y: i32) {
    x.* = x.* + y;
}

func main() {
    var a = 12;
    add_inplace(&a, 12);

    var f = &divmod;
    var a, b = f(10, 5);
}
