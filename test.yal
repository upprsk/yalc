module main;

func main(argc: i32, argv: [*][*]u8) i32 {
    if argc != 2 {
        c_printf("usage: %s <name>\n".ptr, argv[0]);
        return 1;
    }

    c_printf("Hello, %s!\n".ptr, argv[1]);

    return 0;
}

// vim: ft=yal
