module main;

@export
func main() {
    var i = c_getchar();
    if i > 0x30 {
        c_print_cstr("larger than '0'".ptr);
    } else {
        c_print_cstr("smaller or equal than '0'".ptr);
    }

    c_print_cstr("DONE".ptr);

    return;
}
