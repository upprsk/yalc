module main;

func main(argc: i32, argv: [*][*]u8) i32 {
    var count = read_count();
    var arr   = allocate_array(count);
    defer c_free(arr.ptr);

    fill_array(arr);
    print_array(arr);

    return 0;
}

// vim: ft=yal

