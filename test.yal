module main;

def KB    = 1024;
def BUFSZ = KB;

def Counter = struct {
    start: s32 = 0;
    end:   s32;
};

// def SP = struct($T: type) {};

func main() {
    def Local_Struct = struct {
        a: s32;
        b: struct {
            ls: s32;
        };
    };
}

// vim: ft=yal
