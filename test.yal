extern "__builtin_print_i32"
    printi: .(n: i32) -> void;

extern "__builtin_print_char"
    printc: .(c: i32) -> void;

extern "__builtin_read_i32"
    readi: .() -> i32;

main := .() {
    count := readi();

    i := 0;
    while i < count {
        printi(i);
        printc(10);

        i = i + 1;
    }

    printc(10);
};
