module main;

func add_ptrs(a: *const s32, b: *const s32) s32 {
    return a.* + b.*;
}

func add_all(s: []const s32) s32 {}

func noice() (s32, s32) {
    return 0, 1;
}

func main() {
    var a, b = noice();

    var x = 10 + a;
    var y = 20 + b;
    var z = add_ptrs(x&, y&);
}

// vim: ft=yal
