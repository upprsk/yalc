extern "__builtin_print_i32"
    printi: .(n: i32) -> void;

extern "__builtin_print_char"
    printc: .(c: i32) -> void;

extern "__builtin_read_i32"
    readi: .() -> i32;

main := .() {
    len := 2;
    arr := [_]i32{32, 10};

    i := 0;
    while i < len {
        printi(arr[0]);
        printc(10);

        i = i + 1;
    }
};
