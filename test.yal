Pair := .(T: type) -> type {
    return record {
        first: T;
        second: T;
    };
};

main := .() {
    p: Pair(i32) = .{.first = 0, .second = 0};
};

// TODO: Implement variadic functions
extern "printf" printf: .(fmt: [*]i8, arg0: i32) -> void;
