module main;

func main(argc: i32, argv: [*][*]u8) i32 {
    var args = argv[1:argc];

    var i: usize = 0;
    while i < args.len {
        defer i = i + 1;
        c_printf("args[%d] = %s\n".ptr, i, args[i]);
    }

    return 0;
}

// vim: ft=yal
