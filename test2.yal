// c_str :: [*:0]const u8;
// c_int :: i32;
// 
// FILE :: opaque record {};
// 
// extern stdout: *FILE = ---;
// extern stderr: *FILE = ---;
// 
// extern printf:  .(format: c_str, ...) c_int ---;
// extern fprintf: .(FILE: *FILE, format: c_str, ...) c_int ---;

f :: .(x: i32) i32 {
    off :: 4;
    return x + off;
};

// is_zero :: .(i: i32) bool { return i == 0; };

main :: .() {
    // if !is_zero(0) {
    //     return 1;
    // }

    return 0;
};
